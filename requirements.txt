discord.py==2.0.0    # The main library for interacting with the Discord API
PyYAML==6.0          # Used for reading configuration files (e.g., config.yaml)
fpdf==1.7.2          # For PDF report generation
